# Program ID:84 module for EMETH node

## Build

```
$ docker build -t emeth-module-0084:latest
```

## Run

```
$ docker run -v <input directory>:/input -v <output directory>:/output emeth-module-0084:latest <jobId> /input /output
```
